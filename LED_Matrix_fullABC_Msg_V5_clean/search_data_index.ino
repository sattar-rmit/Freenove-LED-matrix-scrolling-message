// 
void search_array_index(char  short_msg[])



{


  int length = number_char_in_msg; 
  int update_disp_array=0;
  int char_position_found=0;

for (int kk = char_position_found;  kk < length;    kk++){


if (short_msg[kk] == ' ')
        {
          Serial.print("---"); Serial.print("+++++");   Serial.println("---");
          Display_array_index[update_disp_array] = 00;
          update_disp_array++;
          char_position_found++;
          // break;
        }


      if (short_msg[kk] == 'A')
        {
          Serial.print("---"); Serial.print("+++++");   Serial.println("---");
          Display_array_index[update_disp_array] = 88;
          update_disp_array++;
          char_position_found++;
          // break;
        }


        if (short_msg[kk] == 'B')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 96;
          update_disp_array++;
          char_position_found++;
          // break;
        }


        if (short_msg[kk] == 'C')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 104;
          update_disp_array++;
          char_position_found++;
          // break;
        }

        if (short_msg[kk] == 'D')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 112;
          update_disp_array++;
          char_position_found++;
          // break;
        }


        if (short_msg[kk] == 'E')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 120;
          update_disp_array++;
          char_position_found++;
          // break;
        }



        if (short_msg[kk] == 'F')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 128;
          update_disp_array++;
          char_position_found++;
          // break;
        }

          if (short_msg[kk] == 'G')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 136;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'H') 
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 144;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'I')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 152;
          update_disp_array++;
          char_position_found++;
          // break;
        }


            if (short_msg[kk] == 'J')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 160;
          update_disp_array++;
          char_position_found++;
          // break;
        }



            if (short_msg[kk] == 'K')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 168;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'L')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 176;
          update_disp_array++;
          char_position_found++;
          // break;
        }


            if (short_msg[kk] == 'M')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 184;
          update_disp_array++;
          char_position_found++;
          // break;
        }


            if (short_msg[kk] == 'N')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 192;
          update_disp_array++;
          char_position_found++;
          // break;
        }


            if (short_msg[kk] == 'O')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 200;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'P')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 208;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'Q')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 216;
          update_disp_array++;
          char_position_found++;
          // break;
        }


            if (short_msg[kk] == 'R')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 224;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'S')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 232;
          update_disp_array++;
          char_position_found++;
          // break;
        }


            if (short_msg[kk] == 'T')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 240;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'U')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 248;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'V')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 256;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'W')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 264;
          update_disp_array++;
          char_position_found++;
          // break;
        }

    if (short_msg[kk] == 'X')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 272;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'Y')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 280;
          update_disp_array++;
          char_position_found++;
          // break;
        }

            if (short_msg[kk] == 'Z')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 288;
          update_disp_array++;
          char_position_found++;
          // break;
        }


             if (short_msg[kk] == '.')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 296;
          update_disp_array++;
          char_position_found++;
          // break;
        }


             if (short_msg[kk] == ',')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 304;
          update_disp_array++;
          char_position_found++;
          // break;
        }


             if (short_msg[kk] == '!')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 312;
          update_disp_array++;
          char_position_found++;
          // break;
        }

             if (short_msg[kk] == '?')
        {
          // Serial.print("---"); Serial.print(int(short_msg.indexOf('A')));   Serial.println("---");
          Display_array_index[update_disp_array] = 320;
          update_disp_array++;
          char_position_found++;
          // break;
        }

        number_char_in_msg = char_position_found;   
        
  }


}
    
